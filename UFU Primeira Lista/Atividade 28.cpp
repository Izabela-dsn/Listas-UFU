#include <stdio.h>
#include <conio.h>

	int main(){
		float a, b, c, q;
		 printf("Coloque tres valores:\n");
		 scanf("%f%f%f", &a, &b, &c);
		  q = (a*a)+(b*b)+(c*c);
	     printf("O resultado da soma dos quadrados dos tres valores colocados e: %.2f ", q);
			
			
		
		getch();
		return 0;
	}
