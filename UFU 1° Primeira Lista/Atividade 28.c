#include <stdio.h>
#include <conio.h>
#include <math.h>

int main(){
    float a, b, c, soma;

    printf("Coloque tres valores:\n");
    scanf("%f%f%f", &a, &b, &c);

    soma = pow(a,2)+pow(b,2)+pow(c,2);

    printf("O resultado da soma dos quadrados: %.2f ", q);

    getch();
    return 0;
}
