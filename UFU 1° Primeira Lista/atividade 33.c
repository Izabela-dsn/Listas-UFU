#include <stdio.h>
#include <conio.h>

    int main() {
    int l, a;
    printf("Coloque a medida do lado do quadrado: ");
    scanf("%i", &l);
    a = l * l;
    printf("a area do quadrado e: %i", a);



	getch();
    return 0;
    }
