#include <stdio.h>
#include <conio.h>

	int main(){
		
	 	int N;
	 	printf("Coloque um numero: ");
	 	scanf("%d", &N);
	 	for( ; N>=N; N=N-2){
	 		if (N%2!=0 && N>=0){
	 			printf("\t%i", N);
	 			
				}
	 }
	 getch();
	 return 0;
	}
